# CLAUDE.md (台灣法律技能包開發指南)

## 專案規範與命令

本專案為 Prompt-Only 台灣法律技能包，不含編譯代碼，主要命令以 Git 管理與測試案例驗證為主。

### 常用命令
*   **Git 狀態查看**：`git status`
*   **Git 提交**：`git add <file>; git commit -m "<msg>"`
*   **檢視所有技能**：在 IDE 中重新載入 `.agents` 目錄。
*   **同步共用規則副本**：`python scripts/sync_agents_rules.py`（修改 `.agents/AGENTS.md` 後必跑；`--check` 供一致性驗證）。

---

## 程式與文件規範

### 1. 繁體中文法律語系限制
所有技能 Prompt、說明文件、測試案例，以及 Agent 回答，**必須**使用中華民國（台灣）法律專業術語。
*   **正確使用**：
    *   「合約」或「契約」（不使用「合同」）
    *   「訴狀」或「起訴狀」（不使用「起訴書」，在台灣起訴書是檢察官起訴刑事被告的文件）
    *   「答辯狀」（不使用「辯護詞」）
    *   「違約金」（不使用「罰款」）
    *   「解除契約」或「終止契約」（不使用「解除合同」）

### 2. 重要變數與說明註解
若在未來子項目中引入輔助程式碼，所有程式碼必須符合：
*   **註解語系**：必須使用繁體中文。
*   **函式級別註解**：每個函式均須有清楚的中文說明，說明其輸入、輸出及法律邏輯對應關係。
*   **重要變數註解**：關鍵變數（如法律關係主體、請求權時效等）需加上註解。

### 3. 單一任務與職責分離原則
*   新增技能時，每個技能資料夾必須獨立且職責單一。
*   嚴禁在單一 `SKILL.md` 中塞入不相關的法律範疇。
